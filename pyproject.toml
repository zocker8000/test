[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kickbase-ranking-bot"
version = "0.1.0"
description = "Scaffold for a deterministic Bundesliga player ranking bot."
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
include = ["kickbase*", "clients*"]
where = ["src"]
