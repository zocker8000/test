# Secrets

This directory is intentionally kept out of version control.

- Put your real OpenAI API key into `openai_api_key.txt`
- The file may contain a placeholder in shared copies
- Do not commit real credentials here
- Use `config/llm.yaml.example` as the shareable config template
